using System;
using UnityEngine;
public class Utls
{
//     public static float GetAngel(Vector3 tgr)
//     {
//         var dis = Math.Sqrt(Math.Pow(tgr.x, 2) + Math.Pow(tgr.z, 2));
//         var agl = Math.Acos(tgr.z / dis) / Math.PI * 180;
//         if (tgr.x < 0)
//         {
//             agl = 360 - agl;
//         }
//         return (float)agl;
//     }
//     public static float GetAngel(Vector3 org, Vector3 tgr)
//     {
//         return GetAngel(tgr - org);
//     }

}
